// Global Jest setup. Reanimated's Jest mock is added here once we have tests
// that render animated components (Phase 4+):
//   require('react-native-reanimated').setUpTests();
// It's intentionally omitted for now — requiring Reanimated eagerly tries to
// initialize native worklets, which fails under jsdom.
